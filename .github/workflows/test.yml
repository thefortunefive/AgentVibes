name: Test Suite

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        node-version: ['18', '20']

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        run: npm install

      - name: Install BATS and Bash 4+
        run: |
          if [[ "$RUNNER_OS" == "Linux" ]]; then
            sudo apt-get update
            sudo apt-get install -y bats
          elif [[ "$RUNNER_OS" == "macOS" ]]; then
            # Install bash 5.x (required for associative arrays in tests)
            brew install bash bats-core
            # Add to PATH
            echo "$(brew --prefix)/bin" >> $GITHUB_PATH
          fi

      - name: Run tests
        env:
          ELEVENLABS_API_KEY: test_mock_key
          AGENTVIBES_TEST_MODE: "true"
        run: |
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            # Ensure BATS uses bash 5.x for associative array support
            export PATH="$(brew --prefix)/bin:$PATH"
            echo "Using Bash version: $(bash --version | head -n 1)"
          fi
          npm test

      - name: Report test results
        if: always()
        run: |
          echo "✅ Test suite completed on ${{ matrix.os }} with Node ${{ matrix.node-version }}"
          echo "See test results above"
