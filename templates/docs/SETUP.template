# {{theme}} Team Setup Guide

This guide will help you set up and configure your {{themeEmoji}} **{{theme}}** themed development team.

## 📋 Prerequisites

### Required Software
- **Node.js 18+** - For running JavaScript agents
- **Git** - Version control (with SSH key configured)
- **GitHub CLI** - For project board integration
  ```bash
  # Install GitHub CLI
  # macOS: brew install gh
  # Ubuntu: sudo apt install gh
  # Windows: winget install GitHub.cli
  ```
- **Docker & Docker Compose** - For containerized environments
- **jq** - For JSON processing in scripts
  ```bash
  # Install jq
  # macOS: brew install jq
  # Ubuntu: sudo apt install jq
  # Windows: winget install jqlang.jq
  ```

### Optional Software
- **Python 3.8+** - If using Python agents
- **Go 1.19+** - If using Go agents

## 🔐 Authentication Setup

### GitHub CLI Authentication
```bash
# Login to GitHub
gh auth login

# Verify authentication
gh auth status

# Test access to your repository
gh repo view {{owner}}/{{repo}}
```

### SSH Key Setup (Recommended)
```bash
# Generate SSH key if you don't have one
ssh-keygen -t ed25519 -C "your_email@example.com"

# Add to SSH agent
ssh-add ~/.ssh/id_ed25519

# Add public key to GitHub
gh ssh-key add ~/.ssh/id_ed25519.pub --title "{{theme}} Team Development"

# Test SSH connection
ssh -T git@github.com
```

## 🚀 First Time Setup

### 1. Install Dependencies
```bash
# For each agent, install dependencies
{{#each agents}}
cd agents/{{id}}
npm install  # or pip install -r requirements.txt for Python
cd ../..
{{/each}}
```

### 2. Environment Configuration
Create `.env` files for each agent with your specific settings:

{{#each agents}}
#### {{emoji}} {{name}} (.env)
```bash
# Navigate to agent directory
cd agents/{{id}}

# Copy example environment file
cp .env.example .env

# Edit with your settings
nano .env
```

Required environment variables:
```env
GITHUB_TOKEN=your_github_token_here
DISCORD_TOKEN=your_discord_token_here  # if using Discord integration
NODE_ENV=development
AGENT_NAME={{name}}
AGENT_EMOJI={{emoji}}
```
{{/each}}

### 3. GitHub Project Board Setup

{{#if hasProjectBoard}}
Your team is configured with project board integration:

**Project ID**: `{{projectId}}`
**Board URL**: https://github.com/{{owner}}/{{repo}}/projects

The board is automatically configured with these columns:
- 📋 **Todo** - New issues and tasks
- 🚀 **In Progress** - Active work
- ⏸️ **Paused** - Temporarily blocked
- 👀 **Ready for Review** - PRs awaiting review
- 🧪 **Testing PR** - PRs being tested
- 🔄 **Rework** - PRs needing changes
- ✅ **Merge PR** - Approved PRs
- ✨ **Done** - Completed work

#### Test Project Board Access
```bash
# Check project access
gh project view {{projectId}}

# List current items
gh project item-list {{projectId}} --owner {{owner}}
```
{{else}}
Project board integration is not configured. To enable:
1. Re-run the team generator with `--project-board` flag
2. Or manually set up project board integration in each agent's CLAUDE.md
{{/if}}

## 🐳 Docker Setup

### Build All Agent Containers
```bash
# Build individual agent containers
{{#each agents}}
cd agents/{{id}}
docker build -t {{../theme}}-{{id}} .
cd ../..
{{/each}}

# Or build with Docker Compose
cd docker
docker-compose -f docker-compose.{{theme}}.yml build
```

### Network Configuration
```bash
# Create Docker network for the theme
docker network create {{dockerNetwork}}

# Verify network creation
docker network ls | grep {{dockerNetwork}}
```

## 🔧 Configuration Verification

### Test Individual Agents
{{#each agents}}
#### {{emoji}} {{name}}
```bash
cd agents/{{id}}

# Test build
./build.sh

# Test launch (dry run)
echo "Testing {{name}} launch..."
./launch

# Check if services are responding
curl http://localhost:{{ports.backend}}/health
curl http://localhost:{{ports.frontend}}

# Stop the agent
./down
```
{{/each}}

### Test Team Coordination
```bash
# Launch all agents
./scripts/launch-all-teams.sh

# Wait for startup (30 seconds)
sleep 30

# Check all services
./scripts/status-check.sh

# Test project board access from each agent
{{#each agents}}
cd agents/{{id}}
/check-board
cd ../..
{{/each}}

# Stop all agents
./scripts/down-all-teams.sh
```

## 📊 Monitoring Setup

### Health Checks
Each agent exposes health endpoints:
{{#each agents}}
- **{{emoji}} {{name}}**: http://localhost:{{ports.backend}}/health
{{/each}}

### Logging
Logs are available in multiple formats:
```bash
# Individual agent logs
cd agents/[agent-name]
tail -f logs/agent.log

# Docker logs
docker logs {{theme}}-[agent-name]

# Docker Compose logs
cd docker
docker-compose -f docker-compose.{{theme}}.yml logs -f [agent-name]
```

## 🔧 Common Issues & Solutions

### Port Conflicts
```bash
# Find what's using a port
lsof -i :{{agents.0.ports.backend}}

# Kill conflicting processes
lsof -ti:{{agents.0.ports.backend}} | xargs kill

# Or use the provided scripts
./scripts/down-all-teams.sh  # This cleans up all ports
```

### Build Failures
```bash
# Clean build cache
{{#each agents}}
cd agents/{{id}}
rm -rf node_modules .next dist build
npm install
cd ../..
{{/each}}

# Rebuild everything
{{#each agents}}
cd agents/{{id}}
./build.sh
cd ../..
{{/each}}
```

### GitHub CLI Issues
```bash
# Re-authenticate
gh auth logout
gh auth login

# Check permissions
gh api user

# Test repository access
gh repo view {{owner}}/{{repo}}
```

### Project Board Issues
```bash
# Verify project exists
gh project view {{projectId}}

# Check permissions
gh api graphql -f query='{ viewer { login } }'

# Refresh project data
{{#each agents}}
cd agents/{{id}}
/update-project-board
cd ../..
{{/each}}
```

## 🎯 Next Steps

After setup is complete:

1. **Create your first issue**:
   ```bash
   cd agents/{{agents.0.id}}
   /create-issue "Set up development environment"
   ```

2. **Start working on it**:
   ```bash
   /start-issue 1  # Use the issue number from step 1
   ```

3. **Check project board**:
   ```bash
   /check-board
   ```

4. **Begin development** with your themed agents!

## 🆘 Getting Help

- **Documentation**: Check the `docs/` directory for detailed guides
- **Logs**: Review agent logs in `agents/[name]/logs/`
- **GitHub Issues**: Create issues using the project board
- **Community**: Join discussions in your repository's issues

---

{{#each agents}}
**{{emoji}} {{name}} says**: *"{{#each personality.catchphrases}}{{#if @first}}{{this}}{{/if}}{{/each}}"*
{{/each}}

**Ready to develop with the power of {{theme}}!** 🚀

---

*Generated by Agentic Team Generator v1.0.0*